# 本目录 (`ele-autotesting/.env`) 用于本地 wrangler dev 的 secrets 注入：
#   packages/server/package.json 的 `dev` 脚本通过 `wrangler dev --env-file ../../.env`
#   显式加载本文件 (相对 packages/server, `../../` 即 ele-autotesting 根目录).
# 生产 secrets 通过 `wrangler secret put <NAME>` 写入 Cloudflare。

# Confluence (用于服务端拉取页面 HTML)
# QA_ALTASSIAN_API_KEY=
# QA_ALTASSIAN_EMAIL=

# Vision API 凭证 (用于图像理解 / OCR)
# QA_IMAGE_RESEARCH_OPENAI_API_KEY=
# QA_IMAGE_RESEARCH_OPENAI_VISION_MODEL=gpt-4o
# QA_IMAGE_RESEARCH_GEMINI_API_KEY=
# QA_IMAGE_RESEARCH_GEMINI_VISION_MODEL=gemini-2.5-flash

# 本地兜底：当本地 Docker / OrbStack 与 wrangler container sidecar 兼容性出问题
# (e.g. setsockoptint EOPNOTSUPP)，设置此变量让 `/mcps/markitdown/*` 反代到外部 URL。
# 生产环境不应设置。
# MARKITDOWN_DEV_URL=https://markitdown-mcp.fan-yang2019.workers.dev

# LLM 提供商 (OpenAI / Gemini / Custom) 的 API Key、Base URL、Model
# 在前端 UI 的「模型管理」中配置, 写入浏览器 localStorage, 不在此处管理。

# 本地 dev 兜底邮箱: wrangler dev 不经 Cloudflare Access, 缺 `cf-access-jwt-assertion`
# 头时 resolveOwner 用 google:<DEV_FALLBACK_EMAIL> 作 ownerId. 仅本地有效, 生产严禁设置.
# 注意: smoke (packages/server/scripts/smoke.mjs) 也走这个 owner; 它已自我隔离到 SMOKE_KEYS,
# 不会清空真实数据, 但用真人邮箱时 smoke 残留 keys 会落到该用户的云端配置里.
# DEV_FALLBACK_EMAIL=smoke@elestyle.jp
