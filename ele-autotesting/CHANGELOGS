# 2026.05.19 (lockstep v1.6.9)

1. 回滚 `.github/workflows/autotesting.yml` 全部 cache 优化改动, 还原到 v1.5.2 (commit b213aa8) 干净状态 (66 行). 删除范围:
   - v1.6.3 引入: `docker/setup-buildx-action@v3` 的 `with: install: true` 选项; `Restore buildx layer cache` step (actions/cache@v4, key `buildx-markitdown-${hashFiles}`, restore-keys 兜底); `Warm buildkit cache` step (`docker buildx build --cache-from type=local --load`); `Persist buildx layer cache` step (`if: always()`, `docker buildx build --cache-to type=local,mode=max --output type=cacheonly`).
   - v1.6.6 引入: `on.push.branches: [main]` trigger; 各 step 的 `if: startsWith(github.ref, 'refs/tags/')` condition; Persist step 加的 `--cache-from`.
   - v1.6.8 引入: `actions/checkout@v5` 的 `fetch-depth: 0`; `Detect markitdown changes since last release tag` step (git diff prev_tag..HEAD); `Cache last-deployed markitdown image URI` step (actions/cache@v4 + URI file); `Patch wrangler.jsonc to reuse cached image` step (jq patch `containers[0].image` 为 registry URI); `Capture deployed image URI (rebuild path only)` step (从 deploy log grep tag + 拼 URI + 写 cache file); 各步骤的 `if: steps.patch.outputs.patched != 'true'` condition.

2. 还原后 autotesting workflow 流程简化为: `actions/checkout@v5` → `Verify tag matches package.json version` → `pnpm/action-setup@v4` → `actions/setup-node@v4` → `Install` (pnpm install --frozen-lockfile) → `Build (core + ui + web)` (pnpm run build:cf) → `docker/setup-buildx-action@v3` (默认无 install) → `Apply D1 migrations (remote)` → `Deploy Worker + Container` (wrangler deploy). 每次 release tag push 完整 cold build markitdown container + push image, 不复用任何 layer cache, 不跳过任何步骤. autotesting tag run 耗时回到 cache 优化前的 baseline (~130-200 s 区间, 取决于 Cloudflare Containers registry layer push 时长波动).

3. cache 方案被用户明确叫停的原因汇总 (供未来参考): (a) v1.6.6 buildx layer cache 加 main scope fallback 后, v1.6.6 vs 历史 cache miss baseline 节省仅 ~30-50 s, 在 130-200 s 自然波动区间内难以感知 — 我前期对比口径不公平 (拿 v1.6.5 的 180 s 慢路径对比 v1.6.6 的 133 s 快路径, 实际历史 v1.6.0 116 s / v1.6.3 第二次 125 s 都比 v1.6.6 还快). (b) v1.6.8 image URI cache 方案设计上有 GitHub Actions cache ref scope 隔离的根本缺陷: cache 写到 tag scope 后, 下一个 tag 的 workflow 看不到 (只能 fallback 读 default branch main scope, 但 main 上 workflow 不 deploy 也不 capture URI). 要绕过得引入 R2 / git auto commit 持久化, 复杂度跳级 + 还需要 1-2 次发版验证, ROI 已经倒挂. (c) v1.6.7 / v1.6.8 / v1.6.9 三次 lockstep 占位发版用于实验 cache 路径, 浪费版本号资源, 开发体验差.

4. 版本号 lockstep 同步 1.6.8 → 1.6.9. SPA `base=/autotest/` 经 gateway `/autotest/*` strip 转发的契约不变.

# 2026.05.19 (lockstep v1.6.8)

1. 修复 v1.6.7 引入的 `Capture deployed image URI (rebuild path only)` step condition bug: 原版 `if: startsWith(github.ref, 'refs/tags/') && steps.markitdown.outputs.changed != 'false'`, markitdown/ 未改 (changed=false) 但 image URI cache 首次出现该 hash key (miss) 走 rebuild path 时, 此 step 被错误 skipped, `/tmp/markitdown-image-uri.txt` 从未写入, actions/cache@v4 post-step 报 `Path Validation Error: Path(s) specified in the action for caching do(es) not exist, hence no cache is being saved.` (warning, 不 fail workflow). 因此 v1.6.7 autotesting tag run 表面 success 但实际 URI cache 是空的, 下次发版照样 cache miss + rebuild path. 修正为 `if: steps.patch.outputs.patched != 'true'`, 与 setup-buildx / Restore / Warm / Persist 四步对齐: 凡是走 rebuild path (patch step 未跑成功 = 没走 reuse), 就 capture URI 给下次复用. 与 markitdown changed 状态解耦.

2. 同时去掉 v1.6.6 引入的 `on.push.branches: [main]` trigger 及其相关 condition (Verify tag / Detect / D1 migrations / Deploy 几步原本要 `if: startsWith(github.ref, 'refs/tags/')` 防 main run 误触, 现 main trigger 没了, 这些 if 全删掉简化 yaml). 设计回顾: main trigger 当初是为了把 buildx layer cache 写到 `refs/heads/main` scope 给 tag push 走 fallback restore (实测 v1.6.6 主键 hit / v1.6.7 主键 hit 都通过这条路径), 但 image URI cache 上线后, markitdown 未改时 wrangler 直接复用 image, 整段 docker 步骤 (setup-buildx / Restore / Warm / Persist) 都 skip, buildx layer cache 完全不参与; 仅在 markitdown 真改 + rebuild path 时 buildx layer cache 才有边际价值 (从其他 ref scope 部分 fallback), 而 main trigger 每次 push main 后台跑 ~80 s 的成本远高于这点边际收益, 删除.

3. 版本号 lockstep 同步 1.6.7 → 1.6.8. v1.6.8 autotesting tag run 期望: markitdown/ 未改 (changed=false), image URI cache key `markitdown-image-uri-c4f421443f...` 在 `refs/heads/refs/tags/v1.6.8` scope 首次出现 (miss, restore-keys 也无, 因为 v1.6.7 那次没写成功), patch step skip (cache-hit=false) → 走 rebuild path → setup-buildx / Restore / Warm / Deploy (cold push image) / Capture URI (修正后正常 grep tag 写 file) / Persist 全跑, 总耗时仍 ~130-160 s, 与 v1.6.6 / v1.6.7 持平. **关键差异**: 这次 actions/cache@v4 post-step 找到 `/tmp/markitdown-image-uri.txt` 实际存在并上传到 cache scope. v1.6.9 起 lockstep 占位发版 (markitdown 未改) image URI cache 主键 hit, jq patch wrangler.jsonc 为 URI, wrangler deploy 跳过 docker build + image push (Building image step skip, push image step skip), autotesting tag run 预期 ~30-50 s 完成. SPA `base=/autotest/` 经 gateway `/autotest/*` strip 转发的契约不变.

# 2026.05.19 (lockstep v1.6.7)

1. 版本号 lockstep 同步 1.6.6 → 1.6.7. 触发 `4df99e9` (`fix(ci/autotesting): skip container build/push when markitdown unchanged`) 的 rebuild path 跑一次, seed 新的 markitdown image URI cache. 改动复述: workflow `actions/checkout@v5` 加 `fetch-depth: 0` 拉 full history + tags; 新 step `Detect markitdown changes since last release tag` 用 `git diff --name-only "$prev..HEAD" -- ele-autotesting/containers/markitdown/` 判断 changed; 新 step `Cache last-deployed markitdown image URI` 用 actions/cache@v4 (key 含 `hashFiles('ele-autotesting/containers/markitdown/**')`, path `/tmp/markitdown-image-uri.txt`); 新 step `Patch wrangler.jsonc to reuse cached image` 仅 changed=false + cache hit 时 jq 把 `packages/server/wrangler.jsonc` 的 `containers[0].image` 从 Dockerfile 路径换成 URI (wrangler config-schema: "The path to a Dockerfile, or an image URI for the Cloudflare registry."); `setup-buildx` / `Restore buildx layer cache` / `Warm buildkit cache` / `Persist buildx layer cache` 四步加 `if: steps.patch.outputs.patched != 'true'` 在 reuse path 整段跳过; 新 step `Capture deployed image URI (rebuild path only)` 从 `/tmp/deploy.log` grep `Building image .+:<tag>` 解析 tag, 用 `${{ secrets.CLOUDFLARE_ACCOUNT_ID }}` 拼完整 URI 写入 cache file (deploy log 里 account id 被 Actions 自动 mask 成 `***`, tag 不被 mask).

2. 本次 v1.6.7 markitdown/ 内容未改, 但 image URI cache key 是首次出现 (上次 deploy 是 v1.6.6 时此 cache 还不存在), Restore 步骤 cache miss → 走 rebuild path: 正常 setup-buildx + Restore buildx layer cache (fallback hit main scope) + Warm buildkit cache (`#7 #8 CACHED`) + wrangler deploy 正常 build + push image, deploy 后 `Capture deployed image URI` step 写入 URI cache. 总耗时与 v1.6.6 (133 s) 大致持平. v1.6.8 起 lockstep 占位发版 (markitdown 未改) image URI cache 主键 hit, 走 reuse path, wrangler.jsonc 被 patch 为 URI, wrangler deploy 跳过 docker build + image push (Building image 步骤直接 skip, push image 步骤 skip), autotesting tag run 预期 ~30-50 s 完成. 若 markitdown/ 真实改动 (Dockerfile / 子文件改动), `hashFiles` 主键变 → cache miss → 自动回到 rebuild path 重新 build + push + seed 新 URI, 下次未改时再 reuse. SPA `base=/autotest/` 经 gateway `/autotest/*` strip 转发的契约不变.

# 2026.05.19 (lockstep v1.6.6)

1. 版本号 lockstep 同步 1.6.5 → 1.6.6 验证 `d72763e` (`fix(ci/autotesting): share buildx cache via main branch scope`) 在 tag push 流程下生效. 改动回顾: workflow trigger 加 `push.branches: [main]`, `Verify tag matches package.json version` / `Apply D1 migrations (remote)` / `Deploy Worker + Container` 三步加 `if: startsWith(github.ref, 'refs/tags/')` 让 main run 跳过 prod 影响, `Persist buildx layer cache` step 加 `--cache-from type=local,src=/tmp/.buildx-cache` 让 export 复用 Warm 加载的内容. 该 commit push 到 main 已触发首次 main scope cache warm-up (run 26101456149, 2 min 完成, `refs/heads/main` scope 现持 `buildx-markitdown-c4f421443f35f97c31b35afc85faa11e88d17fb1ecee4cb0605d7f23e11fe1f1` key, 386 477 810 bytes, 与之前四个 tag scope `refs/heads/refs/tags/v1.6.2..v1.6.5` 同 hash). 本次 v1.6.6 tag run 期望: Restore step 在 `refs/heads/refs/tags/v1.6.6` scope miss → `restore-keys: buildx-markitdown-` 兜底 fallback 到 `refs/heads/main` scope 命中, Warm step 把 386 MB layer 喂进 buildkitd, wrangler deploy 阶段 wrangler 调 `docker build` (经 buildx shim) 时按 Dockerfile instruction hash 命中已加载 layer, `RUN apt-get update && apt-get install -y --no-install-recommends ffmpeg exiftool ca-certificates ...` (18s in v1.6.5) 和 `RUN pip install --no-cache-dir "markitdown-mcp==0.0.1a4" && pip install --no-cache-dir "markitdown[all]==0.1.5"` (含 onnxruntime / pandas / lxml / azure-* / pdfminer / pdfplumber / mammoth / openpyxl / speechrecognition / youtube-transcript-api 等约 30 个大依赖, 在 v1.6.5 占主要构建时间) 标 CACHED 跳过. 对照基线 v1.6.5 autotesting tag run 26100886395 主要时间卡在 cold markitdown build ~4 min, v1.6.6 期望总时长 ~1-2 min. SPA `base=/autotest/` 经 gateway `/autotest/*` strip 转发的契约不变.

# 2026.05.19 (lockstep v1.6.5)

1. `packages/web/src/App.vue` header `#actions` slot 首位 (`<ThemeToggleUI />` 前) 插入 `<ActionButtonUI icon="🏠" text="返回首页" @click="goHome" />`, `<script setup>` 新增 `const goHome = () => { window.location.href = '/' }`. 用 `window.location.href` 而不是 vue-router 或 SPA 内部跳转: 本项目 SPA 生产 `base=/autotest/` (vite base), 任何 router/path 都会被自动 prefix `/autotest/`, 必须用 `window.location.href = '/'` 让浏览器跳出当前 base 回 gateway 根 (`/`), 由 gateway `home.tsx` 接管渲染 landing. 用 `ActionButtonUI` 复用现有 emoji icon + text 样式 (与 `📝 功能提示词` / `📜 上下文` / `⚙️ 模型管理` / `💾 数据管理` 一致), 不破坏 actions row 视觉. 不动 `MainLayout.vue` (在 `packages/ui` 共享组件, 避免其它消费者被波及). 用户反馈"两个子工程 (AutoPilot + AutoTest) 没有返回按钮, 返回不回来".

2. 版本号 lockstep 同步 1.6.4 → 1.6.5. 上游 gateway 友情链接重做: host 前缀 `agentic-loop-ui` → `harness`, 入口位置从 v1.6.4 footer 末行小字 (`.footer-friend`) 提升为与 AutoPilot / AutoTest 并列的第三张卡片 (`section .cards` 改 `grid-template-columns: repeat(3, 1fr)` + `<= 900px` 2 列 + `<= 640px` 1 列 fallback, 新增 `.card-tag` 11px subtle 圆角小标签嵌在 `<h2>`, 删 `.footer-friend`). 上游 ele-autopilot `app/admin/_components/task-content.tsx` topbar 左侧 `<Space>` 最前面加 `<Button icon={<HomeOutlined />} href="/">返回首页</Button>` (与本项目改动同目的, 互为对照入口). SPA `base=/autotest/` 经 gateway `/autotest/*` strip 转发的契约不变.

# 2026.05.19 (lockstep v1.6.4)

1. 版本号 lockstep 同步 1.6.3 → 1.6.4, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.6.4 一同发布; 本项目无业务改动. 上游 gateway landing footer 新增动态后缀友情链接 `agentic-loop-ui`: `app/routes/home.tsx` loader 加 `deriveFriendLink(origin)` 用 `new URL(request.url).origin` 取当前 host, 把首个 `.` 之前的 label 替换为 `agentic-loop-ui` 保留剩余 host (含端口与后续 label); host 不含 `.` (e.g. `localhost:5173`) 或匹配 IPv4 `/^\d+\.\d+\.\d+\.\d+(:\d+)?$/` 时返回 `null` 短路不渲染, 避免开发态错链 (e.g. `agentic-loop-ui.0.0.1:5173`); 配 `app/app.css` `.footer-friend` (`flex-basis: 100%` 配合 `.footer flex-wrap: wrap` 独占第二行, `font-size: 11px` 低于主行 12px, `color: var(--fg-subtle)`, `<a>` underline `text-decoration-color: var(--border)` + `text-underline-offset: 2px`, hover 切 `var(--accent)`) 渲染在 footer 末行, `<a>` 带 `target="_blank"` + `rel="noopener noreferrer"`. 实际访问 host 为 `ele-qa-autopilot.<domain>` 时友链指向 `https://agentic-loop-ui.<domain>` (后缀实时跟随当前域名, 不写死). SPA `base=/autotest/` 经 gateway `/autotest/*` strip 转发的契约不变.

# 2026.05.19 (lockstep v1.6.3)

1. `.github/workflows/autotesting.yml` 接入 buildx layer cache 跨 workflow run 持久化, 削掉 `MarkitdownContainer` 镜像每次 deploy 重跑 `apt-get install` / `pip install` 的耗时. 改动要点: (a) `docker/setup-buildx-action@v3` 加 `with: install: true`, 让 `docker build` shim 到 `docker buildx build`, 这样 wrangler 4 内部走 `docker build` 推 Cloudflare Containers 时也自动走 BuildKit, 才能命中 daemon 内的 layer cache (wrangler 自己不传 `--cache-from` / `--cache-to`, 必须靠 shim 介入). (b) 新增 `Restore buildx layer cache` 步骤, 用 `actions/cache@v4` 把 `/tmp/.buildx-cache` 跨 run 拉回; cache key `buildx-markitdown-${{ hashFiles('ele-autotesting/containers/markitdown/**') }}`, 精确未命中时 `restore-keys: buildx-markitdown-` 回退到任意前一份, 仍可部分复用 layer. (c) 新增 `Warm buildkit cache` 步骤, 在 wrangler deploy 前手动跑一次 `docker buildx build --cache-from type=local,src=/tmp/.buildx-cache -t markitdown:warmup --load containers/markitdown`, 把磁盘 cache 喂进 buildkitd 内部 state, 保证 wrangler 后续调 `docker build` 时按 instruction hash 命中. (d) 新增 `Persist buildx layer cache` 步骤 (`if: always()`, 跟在 deploy 后), 用 `docker buildx build --cache-to type=local,dest=/tmp/.buildx-cache-new,mode=max --output type=cacheonly containers/markitdown` 把当前 daemon 内 cache (含本次 deploy 新增 layer) 落盘并替换旧目录, 喂给下一次 run. 注: wrangler / D1 migrations apply / `wrangler deploy` 命令本身未改, `pnpm --filter @prompt-optimizer/server exec wrangler ...` 调用链不变; container Dockerfile / `MarkitdownContainer` binding / Worker 路由 / SPA `base=/autotest/` 全部不变. 本项目为唯一带 `containers/markitdown/Dockerfile` 的子工程, 优化只作用于 `autotesting.yml`, 其它三个 workflow 无影响.

# 2026.05.19 (lockstep v1.5.18)

1. 版本号 lockstep 同步 1.5.17 → 1.5.18, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.5.18 一同发布; 本项目无业务改动. 上游修复 v1.5.17 引入的 `ele-autopilot upgrade` 缺省 base URL 报错, 同时回滚 v1.5.18 早期草案的 `~/.config/ele-autopilot/base` 持久化方案 (用户反馈 wheel 工具不应写 `$XDG_CONFIG_HOME`; 且 BASE 是 CF 账号私有子域无法在 wheel build 期写入) — ele-autopilot 渲染的 `install.sh` 在 `uv tool install --reinstall` 成功后改为 unquoted heredoc 把 BASE 字面值烧进 `$HOME/.local/bin/ele-autopilot-upgrade` shim 并 `chmod +x`; ele-autopilot-local `autopilot/cli.py` 删 base 解析链与 `--base` 参数, `upgrade` (alias `update`) 缩减为 `bash ~/.local/bin/ele-autopilot-upgrade`, shim 缺失则 stderr 提示重跑 install.sh. SPA `base=/autotest/` 经 gateway `/autotest/*` strip 转发的契约不变.

# 2026.05.19 (lockstep v1.5.17)

1. 版本号 lockstep 同步 1.5.16 → 1.5.17, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.5.17 一同发布; 本项目无业务改动. 上游变化: (a) `ele-autopilot-local/autopilot/cli.py` 接入 `argparse` 修复 `ele-autopilot --help` 此前被 `uvicorn.run` 吞掉的失效问题, 新增 `--help` / `-h`, `--version` / `-V` (输出 `ele-autopilot 1.5.17`), `upgrade` (alias `update`) 子命令复用 gateway `install.sh` 重装最新 wheel (base URL 优先级 `--base` > 环境变量 `ELE_AUTOPILOT_BASE`, 缺失时 stderr 提示并 exit 2); 默认无参数仍 `uvicorn.run("autopilot.cli:app", host="0.0.0.0", port=8000)`, 完全向后兼容. (b) gateway landing 安装本地 agent 区块说明文案改为面向非开发用户 (去掉 `0.0.0.0:8000` 与外层 `<code>` 标签, 改为 `两步完成. 启动后 AutoPilot 工作台会自动连接本地 agent, 即可派单执行任务.`). SPA `base=/autotest/` 经 gateway `/autotest/*` strip 转发的契约不变.

# 2026.05.19 (lockstep v1.5.16)

1. 版本号 lockstep 同步 1.5.15 → 1.5.16, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.5.16 一同发布; 本项目无业务改动. 上游变化: gateway landing `.install` 框补齐小屏适配 — `<= 640px` `padding 18px 16px`, `.install-head` `meta` 取消 `margin-left: auto` 改 `width: 100%` 独占一行, `.steps li` 加 `min-width: 0` 兜底 grid item 与 `.cmd white-space: nowrap` 长 URL 撑破场景且 `padding 10px 12px`, `.install-desc margin-bottom 14px`; `<= 420px` 极窄屏 `.cmd-row flex-wrap: wrap` + `.cmd flex-basis: 100%` + `.copy margin-left: auto` 让命令独占一行、复制按钮在第二行右侧. SPA `base=/autotest/` 经 gateway `/autotest/*` strip 转发的契约不变.

# 2026.05.19 (lockstep v1.5.15)

1. 版本号 lockstep 同步 1.5.14 → 1.5.15, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.5.15 一同发布; 本项目无业务改动. 上游变化: ele-autopilot `install.sh` 加 `ensure_runtime` 前置, 缺失运行时时静默 bootstrap 并把日志捕获到 `mktemp`, 仅失败时回吐, 用户终端 `info` 文案统一中性话术; gateway landing 安装本地 agent 区块由三步改两步 (Step 1 安装命令、Step 2 启动命令), 删去原独立的运行时安装命令. SPA `base=/autotest/` 经 gateway `/autotest/*` strip 转发的契约不变.

# 2026.05.19 (lockstep v1.5.14)

1. mobile 小屏阅读阻塞收尾, 上接 v1.5.13 已做的 InputPanel 控制面板 `flex-wrap` 与提交按钮 `flex-1 sm:flex-none`. 收尾覆盖以下 2 处:
   - `packages/ui/src/components/TestPanel.vue` 顶部 header (`<h3>内容生成</h3>` + `ModelSelectUI` + `开始生成 →` 按钮) `flex items-center justify-between` 改 `flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between`, 右侧按钮组 `flex items-center gap-2` 加 `flex-wrap`; `生成结果` header (含 `添加数据源` dropdown + 已添加 tag 横向滚动条) `flex items-center justify-between mb-3 flex-none` 改 `flex flex-col gap-2 mb-3 flex-none sm:flex-row sm:items-center sm:justify-between`, dropdown 容器 `ml-4` 加 `sm:` 前缀. 此前 360px 屏: ModelSelect (≥180px) + `开始生成 →` 按钮强行同行排, ModelSelect 被压扁到不可点; `生成结果 + 已添加 tag 滚动条 + 添加数据源 dropdown` 三者同行排, dropdown 弹出层 `absolute right-0 w-48` 在 360px 屏右沿溢出. 改后 360px 屏 header 自动两行 (标题独占第一行, 控件第二行 `flex-wrap` 自然换行).
   - `packages/ui/src/components/InputPanel.vue` 标题行内层 `<div class="flex items-center space-x-4">` (包 optimization-mode-selector + 上下文别名 input `flex-1 min-w-[120px]` + PromptTypeSelector) 改 `flex flex-wrap items-center gap-x-4 gap-y-2`. 此前 360px 屏 + `optimizationMode === 'context'` 时三个 item 强行同行排, 别名输入框被压到 ~80px (放不下 4 个汉字 placeholder), PromptTypeSelector dropdown 触发器被挤截; 改后小屏自动两行 (mode selector + 别名输入第一行, PromptTypeSelector 第二行).

   桌面体验 (`>= 640px`) 完全不变. autotest SPA `MainLayout` 已有的 `flex-col lg:flex-row` + `ActionButton` `max-md:hidden` 文字 + 其他 14 个组件已有的 `sm:` / `lg:` 断点本次不动.

# 2026.05.19 (lockstep v1.5.13)

1. 版本号 lockstep 同步 1.5.12 → 1.5.13, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.5.13 一同发布. 上游 ele-autopilot 把 v1.5.12 遗留的 7 处 mobile 小屏阅读阻塞 (3 个 Modal/Drawer 用 `min(N, vw)` 封顶 + JobDetailPanel 任务列表 max-h 加 `sm:` 前缀 + Descriptions 单列堆叠 + folder-sider Agent 配置 Popover 内容动态宽 + 步骤截图 `<Image>` 加 `max-w-full`) 一并修完.
2. 本项目同主题收尾 1 处: `packages/ui/src/components/InputPanel.vue` 控制面板 `<div class="flex items-center gap-2">` 改 `flex flex-wrap items-end gap-2`, 模板选择 `flex-1 min-w-0` 加 `basis-full sm:basis-0` 小屏整行占一行, 提交按钮 `min-w-[60px]` 加 `flex-1 sm:flex-none` 让小屏按钮拉伸占满行剩余空间; 占位高度块 `h-[20px] mb-1.5` 加 `hidden sm:block` 小屏不再渲染无用对齐空白. 此前 360px 屏上 模型选择 / 模板选择 / 控制按钮组 / 提交按钮 4 个 flex item 强行同行挤压, 模板下拉与按钮文本会被截断; 改后小屏自动两行布局 (模型 + 提交按钮一行, 模板选择独占一行), 桌面 (`>= 640px`) 仍单行紧凑布局.

# 2026.05.19 (lockstep v1.5.12)

1. 版本号 lockstep 同步 1.5.11 → 1.5.12, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.5.12 一同发布; 本项目无业务改动. 上游修复: ele-autopilot admin (`/autopilot`) 与执行历史页 (`/autopilot/preview/:taskId`) 在 mobile 小屏 (< 768px) 阅读阻塞 — 固定宽 Ant Sider 抽屉化, Table 加 `scroll={{ x: 'max-content' }}`, Descriptions column 改 `{ xs: 1, sm: 2 }`, Header `flex-wrap`. autotest SPA 自身经 Tailwind `MainLayout` (`flex-col lg:flex-row`) + `ActionButton` (`max-md:hidden` 文字) + 14 个组件已有的 `sm:`/`lg:` 断点已满足"无障碍信息预览", 本次不动.

# 2026.05.19 (lockstep v1.5.11)

1. `packages/web/public/favicon.ico` 与 ele-autopilot 同步替换为 multi-size 16/32/48 (≈ 7.4 KB); 此 ICO 实际只在 autotest SPA 直接被请求 `/autotest/favicon.ico` 时使用 (Vite build 阶段把 root-relative `/favicon.ico` 改写为带 base 前缀的 `/autotest/favicon.ico`), 修复 Retina 屏上 autotest tab favicon 被放大模糊的问题. SPA 行为契约不变.

# 2026.05.19 (lockstep v1.5.10)

1. SPA 品牌与文案对齐: `packages/web/index.html` `<title>` 由 `提示词优化器` 改为 `QA AutoPilot · AutoTest`, 加 description / theme-color + `<link rel=icon|alternate icon|apple-touch-icon|mask-icon|manifest>` 五件套, 指向 gateway 透传的 `/favicon.svg` / `/favicon.ico` / `/apple-touch-icon.png` / `/site.webmanifest`; `packages/web/src/App.vue` 顶部 title slot 去掉 `Ele QA Test Case v<APP_VERSION>` 版本号显示 (版本号在 gateway landing footer 唯一展示), 改为 `QA AutoPilot · AutoTest`, 同步移除 `const APP_VERSION = __APP_VERSION__` (build-time define 仍保留, 仅 UI 不再透出); `packages/ui/src/components/MainLayout.vue` 默认 title slot 由 `Ele QA Test Case` 改 `QA AutoPilot · AutoTest`, logo `<img alt="Logo">` 改 `alt=""` (装饰图片不朗读 alt); `packages/ui/src/components/PromptTypeSelector.vue` MCP client name 从 `QA Test Case` v0.0.1 改为 `qa-autotest` v1.0, 修正服务端日志品牌字样.
2. 品牌资产替换: `packages/ui/src/assets/logo.png` (608×608) 与 `packages/web/public/favicon.ico` 替换为与 gateway landing brand mark 视觉一致的 蓝紫渐变圆角方 + 白 `Q`; 删除未引用的 `packages/ui/src/assets/logo.jpg` (21 KB) 避免长期堆积.

# 2026.05.19 (lockstep v1.5.9)

1. 版本号 lockstep 同步 1.5.8 → 1.5.9, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.5.9 一同发布; 本项目无业务改动. 上游修复: gateway landing CSS 加 `prefers-reduced-motion` 兜底 (a11y) + `<html lang>` 改 `zh-CN`. gateway `/autotest/*` strip 转发逻辑与本项目 SPA `base=/autotest/` 契约不变.

# 2026.05.19 (lockstep v1.5.8)

1. 版本号 lockstep 同步 1.5.7 → 1.5.8, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.5.8 一同发布; 本项目无业务改动. 上游修复: gateway landing SSR 拿真实 origin 直接渲染 install 命令 (修复占位符闪烁), `/index.html` 由 worker 层 301 → `/` (修复 RR 无该路由的 404), clipboard catch + 删 service binding 上无效的 `cf.cacheTtl`. gateway `/autotest/*` strip 转发逻辑不变.

# 2026.05.19 (lockstep v1.5.7)

1. 版本号 lockstep 同步 1.5.6 → 1.5.7, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.5.7 一同发布; 本项目无业务改动. 上游变化: gateway landing 从单文件内嵌 HTML 重写为 React Router v7 framework mode (SSR), 视觉重做; gateway 路由分发顺序与对 `/autotest/*` 的 strip 转发逻辑不变.

# 2026.05.19 (lockstep v1.5.6)

1. 版本号 lockstep 同步 1.5.5 → 1.5.6, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.5.6 一同发布; 本项目无业务改动. 上游修复: autopilot-local workflow 4 处 `wrangler r2 object put` 补 `--remote` (wrangler v4 默认 local), production R2 写入恢复.

# 2026.05.19 (lockstep v1.5.5)

1. 版本号 lockstep 同步 1.5.4 → 1.5.5, 与上游 gateway / ele-autopilot / ele-autopilot-local v1.5.5 一同发布; 本项目无业务改动. 上游变化: install 入口从 ele-autopilot `/help` 迁到 gateway landing 页内嵌区块.

# 2026.05.19 (lockstep v1.5.4)

1. 修复 `packages/ui/src/components/PromptTypeSelector.vue` 内 4 处 `fetch` 漏 `/autotest` 前缀导致 Confluence 解析 / Figma 解析 / 图片识别 / markdown-research 在生产 gateway 后全部命中 404 (与 v1.5.3 第 5/6 条同型, 上版仅修了 `/api/sync/*` 与 `/stream-proxy /http-proxy`, 本次补齐 `/confluence-parse` / `/figma-parse` / `/image-research/analyze` / `/markdown-research`). 实际请求被 gateway 默认转给 AUTOPILOT, AUTOPILOT 没有这些路由直接 ErrorBoundary 404 HTML.
2. `packages/core/src/utils/environment.ts` 新增 `getApiBasePath()` getter, 读已被 `setProxyBasePath` 写入的 module-level `configuredBasePath`; 由 `packages/core/src/index.ts` + `packages/ui/src/index.ts` re-export. UI 组件直接拼 `${getApiBasePath()}/<path>`, 与 `getProxyUrl` 完全同源, 没有引入新机制.

# 2026.05.19 (lockstep v1.5.3)

1. 版本号 lockstep 同步 1.5.2 → 1.5.3, 与上游 ele-autopilot / ele-autopilot-local / gateway v1.5.3 一同发布.
2. `packages/server/wrangler.jsonc` 加 `workers_dev: false` — Worker 不再暴露 `*.workers.dev` 公网, 仅可经新增 gateway Worker (`qa`) 通过 service binding 访问.
3. `packages/web/vite.config.ts` `base` 选项: 生产构建走 `/autotest/`, dev 保持 `/` — 配合 gateway 把 `/autotest/*` 路径 strip 后转发到本 Worker.
4. `packages/server/src/index.ts` 末尾加 `env.ASSETS.fetch(request)` fallback (仅 Hono 404 时触发) — service binding 调用会绕过平台层 Static Assets 处理, 需要 Worker 内手动 fallback 才能让静态资源 + SPA `not_found_handling` 工作.
5. 修复 Web SPA 调远端存储 API 漏 `/autotest` 前缀导致 404 (`/api/sync/items/*` 等被 gateway 默认转给 AUTOPILOT, AUTOTEST 不接到流量). `useAppInitializer` 改为接收 `apiBase` 参数, web 侧由 `import.meta.env.BASE_URL` 注入 (生产 `/autotest`, dev `''`), 与 vite `base` 锁步. 影响: `packages/ui/src/composables/useAppInitializer.ts` + `packages/web/src/App.vue`.
6. 修复 LLM 流式 / HTTP 代理 (`/stream-proxy`, `/http-proxy`) 同样漏 `/autotest` 前缀导致优化按钮触发 404 (与第 5 条同型). core 包加 `setProxyBasePath(basePath)` module setter, `getProxyUrl` 拼 URL 时带上前缀; `useAppInitializer` 在 setup() 同步阶段调用 setter (早于 `createLLMService`). 影响: `packages/core/src/utils/environment.ts` + `packages/core/src/index.ts` + `packages/ui/src/index.ts` + `packages/ui/src/composables/useAppInitializer.ts`.

# 2026.05.19 (lockstep v1.5.2)

1. 版本号 lockstep 同步 1.5.0 → 1.5.2, 与上游 ele-autopilot / ele-autopilot-local v1.5.2 一同发布; 本项目无业务改动.
2. 工程层撤销 per-project namespace tag (`ele-autotesting/v*`) 回到统一 `v*` lockstep tag: `.github/workflows/autotesting.yml` trigger + verify 改回 `v*`; `Deploy.md` / `CLAUDE.md` tag 格式与 workflow 路径同步. 详见根 `AGENTS.md` "Git / 发布约定".

# 2025.11.10

1. confluence office api upgraded.

# 2025.10.14

1. confluence 文本支持 img 解析。

# 2025.10.10

1. Add “PRD生成（beta）” 模版。
2. Add “Prompt-创世纪” 模版。
3. Add “Prompt-演化纪” 模版。
4. Update: “传话机器人” 提示词增强，小模型也很好的完成任务。
5. Update: 部分文案更新

# 2025.10.07

1. 支持 excel 导出

# 2025.09.30

1. 优化了图片识别的接口，默认使用 gemini 2.5-flash，(支持 openai 和 gemini 两种模型)。
2. 支持文件 markdown 识别（支持 pdf、docx、pptx、csv、doc、epub、html、htm、txt、text、plaintext、xlsx 等文件类型）。

# 2025.09.26

1. 迭代优化的模版，只能选择高级模版，不能选择简单模版。代码层面进行过滤。
2. 切 tab 的时候，保留【内容生成】中的数据，不要清空。

# 2025.09.25

1. 优化【测试用例生成】提示词模版
2. 增加【鹰眼分析】提示词模版
3. 文案修改：用例生成/测试用例生成 => 内容生成/内容生成

# 2025.09.18

1.  增加了 figma 快速录入
    1.  目的：解决图片录入操作繁琐的问题
2.  图片识别增加了 gemini 支持，默认 gemini 2.5-flash。
    1.  目的：识别更准确、速度更快。
3.  测试用例生成提示词作了优化，
    1.  生成的用例质量会更高（覆盖更全面、边界条件等细节会更多）
    2.  删除了将用例进行聚合相关的描述
    3.  生成的用例保持清晰、有序
4.  增加了【传话人】提示词，用于不修改任何 raw 内容，直接进行上下文的录入。
5.  文案修改：用例验证 => 用例生成
