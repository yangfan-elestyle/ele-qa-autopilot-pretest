### example

```
1. 访问目标 URL：https://acq.stg.elepay.dev/merchant/apply-invitation/new
2. 登录检查：检测页面是否存在登录表单（如邮箱/密码输入框）。若存在，使用账号 "xxx@elestyle.jp" 和密码 "xxx" 完成登录；若已登录，直接进行下一步。
3. 页面【加盟店申請新規】：
   - 在 "お客様情報(管理用)" 输入框中填入 "test-1"。
   - 点击 "次へ" 按钮（独立点击操作）。
4. 页面【契約内容】 - "サービス利用有無" 区域（注意：必须按照 YES 和 NO 的说明进行勾选或者不勾选）：
   - 选择项 "クレジットカード" 是否勾选："YES"。
     - 若上一步勾选了：
       - 在 "契約形態" 中分别点击勾选 "VM業代" 和 "JAD業代"（确保分两次点击）。
       - 在 "洗替" 下拉框选择 "手動"。
       - 在 "洗替通知有効期限" 的三个输入框依次填入 "1"、"ー"、"ー"。
   - 选择项 "口座振替" 是否勾选："YES"（如果是 YES，一定要勾选；如果是 NO，一定不要勾选）。
     - 一定要严格检查该项是否完成勾选或不勾选。
   - 选择项 "WEB口座振替受付サービス" 是否勾选："YES"（如果是 YES，一定要勾选；如果是 NO，一定不要勾选）。
     - 一定要严格检查该项是否完成勾选或不勾选。
   - 选择项 "PAYSLE" 是否勾选：YES（务必勾选）。
     - "手数料負担区分" 选择 "企業負担"。
     - "入金サイクル" 选择 "2回"。
5. 页面【契約内容】 - "支払い方法" 区域：
   - 将 "再請求・再登録サービス" 的单选框设置为 "利用する"。
   - 在 "主決済手段選択" 中，如单选项是可选操作，则进行勾选。如不可选，则不予勾选。
   - 在 "再請求有効期限" 的三个输入框依次填入 "2"、"1"、"1"。
   - 点击勾选 "手数料情報登録" 中的 "定価" 选项。
6. 页面【契約内容】 - 底部：
   - "課金開始月" 输入框：严格遵守【时间控件规范】进行时间选择 actions 操作。文本时间输入使用当前时间的下个月月份（示例：当前 2026-02，则输入 2026-03）。
   - 点击 "次へ" 按钮。
7. 页面【招待メールの作成】：
   - 在 "宛先" 输入框填入 "yyy@elestyle.jp"。
   - 点击 "次へ" 按钮。
8. 页面【最終確認】：
   - 滚动到底部，点击 "送信" 按钮。"送信" 按钮成功点击后会，会调整到首页，此时即表示所有任务已经完成，进入 9 结束当前所有任务。
9. 结束所有任务结束，关闭当前浏览器窗口。
```
