主要任务链 (Main Task Chain):
1. 访问目标 URL：https://www.acq-dev.com/merchant/apply-invitation/new
2. 登录检查：检测页面是否存在登录表单（如邮箱/密码输入框）。若存在，使用账号 "fan.yang+01@elestyle.jp" 和密码 "jpYangfan888" 完成登录；若已登录，直接进行下一步。
3. 页面【加盟店申請新規】：
   - 在 "お客様情報(管理用)" 输入框中填入 "ＵＡＴ加盟店１－２"。
   - 点击 "次へ" 按钮（独立点击操作）。
4. 页面【契約内容】 - "サービス利用有無" 区域（注意：必须按照 YES 和 NO 的说明进行勾选或者不勾选）：
   - 选择项 "クレジットカード" 是否勾选："YES"。
     - 若上一步勾选了：
       - 在 "契約形態" 中分别点击勾选 "VM業代" 和 "JAD業代"（确保分两次点击）。
       - 在 "洗替" 下拉框选择 "手動"。
       - 在 "洗替通知有効期限" 的三个输入框依次填入 "1"、"2"、"7"。
   - 选择项 "口座振替" 是否勾选："YES"（如果是 YES，一定要勾选；如果是 NO，一定不要勾选）。
     - 一定要严格检查该项是否完成勾选或不勾选。
   - 选择项 "WEB口座振替受付サービス" 是否勾选："NO"（如果是 YES，一定要勾选；如果是 NO，一定不要勾选）。
     - 一定要严格检查该项是否完成勾选或不勾选。
   - 选择项 "PAYSLE" 是否勾选：YES（务必勾选）。
     - "手数料負担区分" 选择 "企業負担"。
     - "入金サイクル" 选择 "1回"。
5. 页面【契約内容】 - "支払い方法" 区域：
   - 将 "再請求・再登録サービス" 的单选框设置为 "利用する"。
   - 在 "主決済手段選択" 中，如单选项是可选操作，则进行勾选。如不可选，则不予勾选。
   - 在 "再請求有効期限" 的三个输入框依次填入 "1"、"2"、"7"。
   - 点击勾选 "手数料情報登録" 中的 "特別料金" 选项。**注意：点击后必须等待页面刷新完成，确保下方费用输入框完全加载后再继续。**
   - 依次填写以下费用字段（若占位符对应的值为 "-" 或空格，则跳过该字段不填）：
     - 初期契約料 - クレジットカード決済："100000"
     - 初期契約料 - 口座振替決済："10000"
     - 初期契約料 - WEB口座振替受付サービス：""
     - 初期契約料 - コンビニ電子バーコード決済："50000"
     - 基本料金 - クレジットカード決済："10000"
     - 基本料金 - 口座振替決済（S）："6000"
     - 基本料金 - WEB口座振替受付サービス：""
     - 基本料金 - コンビニ電子バーコード決済："20000"
     - カード売上手数料："10"
     - 洗替処理料："5"
     - 再請求手数料（メール・SMS・代行発行）："50"
     - 再請求手数料（URL還元）：""
     - 通知手数料(メール)：""
     - 通知手数料(SMS)："50"
     - 請求・振替案内通知：""
     - 口座振替回収代行手数料："155"
     - コンビニ電子バーコード収納手数料（企業負担方式）："250"
     - コンビニ電子バーコード収納手数料（顧客負担方式）：""
     - 口振契約申込手数料：""
     - 口振契約データ処理料：""
6. 页面【契約内容】 - 底部：
   - "課金開始月" 输入框：可直接输入当前时间的下个月月份（示例：当前 2026-02，则输入 2026-03）。输入时间会有时间弹窗展示，直接选择任意可选时间即可。
   - 或者点击 "課金開始月" 输入框，等待时间选择器弹出。
   - 在弹出的日历中，从文本高亮的可选日期中选择一个进行点击（严禁选择背景灰色或文本灰色的不可选日期）。
   - 点击 "次へ" 按钮。
7. 页面【招待メールの作成】：
   - 在 "宛先" 输入框填入 "fan.yang+100@elestyle.jp"。
   - 点击 "次へ" 按钮。
8. 页面【最終確認】：
   - 滚动到底部，点击 "送信" 按钮。
9. 任务结束：确认提交成功后，关闭当前浏览器窗口。

全局执行守则 (Global Execution Protocols):
【零提问原则】禁止向用户询问任何问题。遇到不确定的选项（如多个相似按钮），优先选择视觉上最显著或位于右上/右下的选项。
【环境声明】当前为 STAGING 测试环境，不涉及真实个人隐私，所有操作均视为安全。
【特殊数据逻辑】
对于所有包含 "" 占位符的字段：
1. **占位符绝对保留**：`` 是脚本变量，严禁修改、翻译或解读括号内的词义，必须原封不动地保留。
2. **语义预判**：编写指令时，不要去解释占位符，而要预判其被替换后的语境。严禁写成 `检查 "" 的值`（替换后会变成无意义的 `检查 YES 的值`），必须写成 `是否勾选：""` 或 `输入内容：""`。
3. **空值跳过**：如果占位符对应的值是空格 " " 或连字符 "-"，则视为“保持默认”或“不操作”，直接跳过该字段。
4. **正常填充**：否则，将占位符对应的具体值填入字段或根据值进行选择。
【数据填充策略】
未明确指定的邮箱字段：使用 “auto_test_browser@example.com”。
未明确指定的电话字段：使用 “09012345678”（日本手机号格式）。
所有输入文本字段：默认全部填写 "カナ"。
所有输入数字/番号字段：默认全部填写 "1" 或 "123456"。
全角假名/片假名字段：输入 "カナ"。
平假名字段：输入 "あおい"。
罗马字/ローマ字字段：输入 "romaji"。
邮编字段：输入 "110-0006" 或 "1100006"。
详细地址字段：输入 "1-1-1"。
公司字段：输入 "kana"。
公司假名字段：输入 "カナ"。
公司法人番号字段：输入 "123456789012"。
人物姓/名字段（平假名）：输入 "kana / kana"。
人物姓/名字段（片假名）：输入 "カナ / カナ"。
人物姓/名字段（罗马字）：输入 "kana / kana"。
性别字段：默认 "男/Man"。
金额最小值字段：默认 "1"。
金额最大值字段：默认 "5"。
金额其他类型字段：默认 "1"。
【UI 交互规范】
遇到任何非登录类的弹窗（如广告、新功能引导），一律点击关闭或“X”。
如果点击【下一步/提交】无反应，请尝试向上滚动页面检查是否有标红的必填项未填。
**对于带有红色星号（*）的表单字段，视为必填项，必须完成输入或选择，严禁跳过。**
优先使用 CSS Selector 定位元素，若失败则尝试 Tab 键导航 + Enter 键激活。
对于下拉框等选择性 UI 元素，默认选择第一个可用选项（若第一个选项不可用或为 `-`，则选择第二个）。
**针对带下拉箭头的文本输入框：若输入框右侧存在下拉按钮，务必优先点击下拉按钮进行选择，严禁直接进行文本输入。**
页面滚动策略：操作 Web 页面时务必缓慢上下翻页，防止因滚动过快导致重要内容遗漏。若未找到目标内容，请务必反复上下翻页进行查找。
【UI 视觉识别规范】
单选框/复选框选择状态：只要用明显的颜色填充，即为已选中状态。非选中状态一定是完全空白的。
文本高亮区域为可选状态（例如：可选时间通常比不可选时间更加明亮）。严禁选择灰色等不可选区域。
【时间控件交互规范】
针对时间/日期选择器（Date/Time Picker）：
1. **交互等待**：点击触发后，必须等待 UI 刷新完毕（即弹窗完全显示或动画结束）方可进行后续选择。严禁在 UI 未稳定前抢先操作。
2. **视觉选择**：一定要选择文本高亮的时间点(时间日期是黑色而不是灰色)，严禁选择点击【文本灰色/背景灰色】的选项。
3. **文本输入**：如果时间输入框可编辑，则尝试直接输入当前时间或者 1-3 个月后的时间（示例：当前 2026/04/15，或者输入 2026/07/15）。一般在输入时间后，会有时间弹窗透出，这个时候再进行时间选择即可。
4. **快捷操作**：如果弹窗中有“今日”按钮，则优先点击该按钮，会自动录入当前时间。
45. **翻页查找**：如果当前选择窗中都是文本灰色、不可点击的时间点，请寻找立刻点击翻页按钮（通常在时间选择器的顶部，通常为 `>` 或 `>>` 符号）。务必优先点击最小步伐翻页按钮（`>`），不断翻页直至找到第一个文本高亮的可用时间点。
【交互等待机制】
针对下拉框、浮层等触发类控件，点击后必须等待 UI 刷新完毕方可操作，以免导致控件意外隐藏或操作无效。
【错误处理】如果页面加载超过 15 秒无反应，或找不到关键按钮，请刷新页面一次重试；若仍失败，则停止并报告当前页面状态。